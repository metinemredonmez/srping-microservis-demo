server:
  port: 8888

spring:
  application:
    name: config-server-git
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/muhammedali55/microservice-egitim-config-server.git"
          default-label: "main"
          skip-ssl-validation: true
          username: "muhammedali55"
          password: ${GIT_KEY}